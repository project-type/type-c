

type Instance = class {
    fn init() {
        return Instance()
    }
}

type Instance2 = interface {
    fn init()
}

type Instance3 = interface {
    fn init()
}


type C = Instance2 & Instance

