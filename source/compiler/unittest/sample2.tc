
type Tree = variant {
    Binary(lhs: Tree, rhs: Tree),
    Unary(child: Tree),
    Leaf(value: u32),
    Empty()
}

type Instance = class {
    fn init() -> Instance{
        return Instance()
    }
}

type Instance2 = interface {
}

type Instance3 = interface {
    fn inits()
}


type C = C

