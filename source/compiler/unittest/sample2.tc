
type Tree = variant {
    Binary(lhs: Tree, rhs: Tree),
    Unary(child: Tree),
    Leaf(value: u32),
    Empty()
}

type Instance = interface {
    fn cool() -> Instance
}

type Instance2 = interface {
    fn cool() -> Instance2
}

type Instance3 = class(Instance, interface{fn cools() -> Instance3 }) {
    fn inits(){print("HAYYA")}
}


type C = C

