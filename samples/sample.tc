/**
 * This is a cool file, trust me
 */
from std import io
from matplotlib import pyplot.plot as pt
import pandas.pandas_api as pd

fn main() {
    let {x, y} = getValue<str>();
    let {z: u32, alpha: i32} = getValue<str>();
}
